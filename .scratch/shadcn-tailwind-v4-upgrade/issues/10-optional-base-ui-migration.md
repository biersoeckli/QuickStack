# 10 — (Optional) Primitives auf Base UI migrieren

**What to build:** Optionale, progressive Migration der Primitives von Radix auf Base UI (shadcn-Default
seit Juli 2026). Kein Muss — Radix bleibt unterstützt. Die App bleibt nach jeder Komponente auslieferbar.

**Blocked by:** 09 — Upgrade integrieren und verifizieren.

**Status:** needs-triage

- [ ] Produktentscheidung dokumentiert: migrieren ja/nein, und warum.
- [ ] Falls ja: `shadcn/ui`-Skill installiert und Migration komponentenweise durchgeführt.
- [ ] Nach jeder migrierten Komponente ist der Build grün und die Komponente manuell verifiziert.
- [ ] Falls nein: Ticket begründet schließen, keine Codeänderung.
